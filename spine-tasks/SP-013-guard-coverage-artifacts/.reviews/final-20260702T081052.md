### Verdict: PASS

### Summary

SP-013 delivers a focused guard script that fails when any path under `extension/coverage/` is tracked in git, warns (without failing) on expected post-test coverage artifacts, and documents the check in `AGENTS.md`. Contract `testCommand` was re-run successfully (85 extension tests; script PASS before and after `npm test`), and the fail path was verified by simulating `git add -f extension/coverage/lcov.info`. Machine contract verification also passed; file scope respected (`cli/**` and `extension/src/**` unchanged).

### Contract verification

| Check | Result |
|-------|--------|
| testCommand | PASS — `bash scripts/check-untracked-coverage.sh && cd extension && npm test && bash ../scripts/check-untracked-coverage.sh` exited 0 |
| fileScopeMustChange (`scripts/check-untracked-coverage.sh`) | PASS — new executable script with tracked-path check and untracked WARN |
| fileScopeMustNotChange (`cli/**`, `extension/src/**`) | PASS — no changes in those paths |

### Completion criteria

- All PROMPT steps marked complete in STATUS with documented behavior (tracked FAIL, untracked WARN-only).
- Script exits 0 on current tree; exits 1 when coverage is force-tracked.
- `AGENTS.md` Testing section documents when to run the guard.

### Verification run

```
bash scripts/check-untracked-coverage.sh  → PASS (WARN on gitignored artifacts)
cd extension && npm test                  → 85 tests passed
bash ../scripts/check-untracked-coverage.sh → PASS (WARN on gitignored artifacts)
```

Fail-path simulation: `git add -f extension/coverage/lcov.info` → script exit 1 with listed tracked path and fix hint.

```json
{"verdict":"PASS","feedback":"Guard script, AGENTS.md hook, and contract testCommand satisfy SP-013 completion criteria. Script correctly fails on tracked extension/coverage paths and passes after extension npm test."}
```
