### Verdict: APPROVE

### Summary

Step 3 delivers the contract `testCommand`, documents auto-finish behavior in `docs/cli-extension-contract.md`, and all scoped tests pass. The SP-004 diff (commits `6542293`–`af3d3a6`) implements opt-in auto-finish on zero active findings across CLI and extension with solid unit coverage on contract deliverables (`finishReview.ts` at 89.6% line coverage) and core CLI helpers (`ActiveFindingCount`, `MaybeAutoFinish` happy/skip paths).

Build (`go build ./...` + `npm run compile`) and the full contract test command succeed with zero failures.

### Build / typecheck

Commands run (from `.spine/spine-config.json`):

- `cd cli && go build ./... && cd ../extension && npm run compile` — exit 0
- Contract `testCommand`: `cd cli && go test ./internal/run/... ./internal/config/... ./cmd/stet/... -count=1 && cd ../extension && npm run compile && npm test` — exit 0 (80 extension tests, all Go packages in scope pass)

### Coverage

`testing.testWithCoverage` equivalent run:

- Extension in-scope files: `finishReview.ts` 89.58%, `cli.ts` 95.93% (both ≥77%)
- CLI new functions: `ActiveFindingCount` 85.7%, `MaybeAutoFinish` 72.7% (happy path and skip path exercised; error branches uncovered)

### Scope and contract alignment

- **In scope:** CLI config/flag/env (`auto_finish_zero_findings`, `STET_AUTO_FINISH_ZERO`, `--auto-finish-zero`), `run.MaybeAutoFinish` / `tryAutoFinishZero`, extension `maybeAutoFinishAfterReview` + `--auto-finish-zero` passthrough, contract doc, tests.
- **Out of scope respected:** `scripts/**` unchanged.
- **Completion criteria:** Zero-findings auto-finish when enabled; opt-out default (`false`); dry-run excluded; active findings (minus dismissed) gate finish; explicit finish unchanged when findings remain.

### Suggestions (non-blocking)

1. **`cli/internal/run/run.go:1716-1735`** — `MaybeAutoFinish` is at 72.7% line coverage; add tests for empty `RepoRoot`/`StateDir`, `ActiveFindingCount` load error, and `Finish` failure to reach the 77% gate on error paths.
2. **Missing test:** `extension/src/finishReview.test.ts` — no case for `list` exiting non-zero (lines 69–70), which currently returns `"skipped"` silently.
3. **`cli/cmd/stet/main.go:105-115`** — `tryAutoFinishZero` gating (disabled config, dry-run) is untested at the command layer; consider a lightweight integration test on `stet start --dry-run` vs `--auto-finish-zero`.

```json
{"verdict":"APPROVE","feedback":"Step 3 verification complete: build and contract testCommand pass; contract doc updated; auto-finish behavior tested on finishReview deliverables and core CLI helpers. Minor error-path coverage gaps in MaybeAutoFinish and finishReview list-failure are non-blocking at review level 2."}
```
