### Verdict: PASS

### Summary

SP-004 is complete: opt-in auto-finish on zero active findings is implemented in CLI (`MaybeAutoFinish`, config/env/flag, `tryAutoFinishZero`) and extension (`maybeAutoFinishAfterReview`, `--auto-finish-zero` passthrough), with explicit finish unchanged when findings remain and dry-run excluded. Contract verification passed; build gates (`go build ./...`, `npm run compile`) and the contract `testCommand` (scoped Go tests + 80 Vitest tests) all succeed. Documentation in `docs/cli-extension-contract.md` describes the feature and config keys.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` | PASS — `go test ./internal/run/... ./internal/config/... ./cmd/stet/... -count=1`, `npm run compile`, `npm test` (80/80) |
| `fileScopeMustChange` | PASS — `extension/src/finishReview.ts`, `extension/src/finishReview.test.ts` updated |
| `fileScopeMustNotChange` | PASS — `scripts/**` unchanged |

### Build / typecheck

- `cd cli && go build ./...` — exit 0
- `cd extension && npm run compile` — exit 0

### Completion criteria

| Criterion | Status |
|-----------|--------|
| All steps (0–3) complete | Yes — STATUS and commits `6542293`–`af3d3a6` |
| Zero-findings auto-finish when enabled | Yes — CLI `MaybeAutoFinish` + extension `maybeAutoFinishAfterReview` |
| Opt-out default | Yes — `auto_finish_zero_findings = false`; enable via config, `STET_AUTO_FINISH_ZERO=1`, or `--auto-finish-zero` |
| Explicit finish when findings remain | Yes — `findings_remain` path; `stet.finishReview` command unchanged |
| Dry-run excluded | Yes — gated in CLI and extension |
| Active findings semantics | Yes — dismissed IDs excluded (`ActiveFindingCount`, `stet list` check) |
| Tests pass | Yes — verified this review |
| Contract doc updated | Yes — `docs/cli-extension-contract.md` §Other commands + config table |

### Coverage (contract deliverables)

| File | Line coverage |
|------|---------------|
| `extension/src/finishReview.ts` | 89.6% |
| `extension/src/cli.ts` | 95.9% |
| `cli/internal/run` package (overall) | 70.4% — pre-existing baseline; new `MaybeAutoFinish` / `ActiveFindingCount` have dedicated unit tests |

Contract-scoped extension modules exceed the 72% per-file threshold. `run.go` package aggregate remains below 77% (tracked separately per project hygiene); not blocking at Review Level 2.

### Suggestions (non-blocking)

1. **`docs/cli-extension-contract.md:88`** — STATUS notes auto-finish on `stet commitmsg --commit-and-review`; contract doc lists only `start` / `run` / `rerun`. Add `commitmsg` for parity.
2. **`extension/src/finishReview.test.ts`** — `maybeAutoFinishAfterReview` does not cover `list` exit-code failure (`finishReview.ts:69-71`); low risk.
3. **`cli/internal/run/run.go:1716-1735`** — `MaybeAutoFinish` error branches (empty opts, `ActiveFindingCount` / `Finish` failure) lack dedicated tests; happy and skip paths are covered.

```json
{"verdict":"PASS","feedback":"All SP-004 completion criteria satisfied: opt-in auto-finish on zero active findings in CLI and extension, opt-out default, dry-run exclusion, explicit finish when findings remain, contract doc updated, and build/test gates pass. Contract machine verifier PASS."}
```
