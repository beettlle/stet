### Verdict: PASS

### Summary

SP-007 delivers display-only confidence and category filtering for the findings panel via `stet.minConfidence` and `stet.categories` settings, with filtered findings hidden from the tree and a panel message showing the hidden count. Implementation stays within file scope, respects the CLI contract (no schema changes, `cli/**` untouched), and includes predicate plus integration tests and README documentation. Contract verification passed and `cd extension && npm run compile && npm test` completed with 93/93 tests passing; `findingsPanel.ts` line coverage is 91.35%.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` (`cd extension && npm run compile && npm test`) | PASS — 93 tests, 0 failures |
| `fileScopeMustChange` (`extension/src/findingsPanel.ts`) | PASS — filter predicates, tree filtering, hidden-count message |
| `fileScopeMustNotChange` (`cli/**`) | PASS — no CLI changes in commit `de5fdcf` |

### Completion criteria

| Criterion | Status |
|-----------|--------|
| All steps complete (0–3) | PASS — STATUS.md documents hide policy, settings, tests, README |
| Confidence and category filters work | PASS — `passesFindingFilters` + `getChildren` filtering; config wired in `extension.ts` |
| Tests pass | PASS — verified independently |

### Build / typecheck

```
cd extension && npm run compile && npm test
```

- `tsc -p ./` — succeeded (no errors)
- Vitest — 6 files, 93 tests passed
- Coverage: `findingsPanel.ts` 91.35% lines (≥72% per-file gate); project 90.71% lines

### Implementation notes

- **Settings:** `extension/package.json` defines `stet.minConfidence` (0–1, default 0) and `stet.categories` (enum allowlist, default empty = show all).
- **Hide policy:** Documented in STATUS; filtered findings omitted from tree; `treeView.message` shows `"N hidden by filter"`.
- **Display-only:** Filters applied at render time; session/CLI state unchanged.
- **Scope:** Changes limited to `findingsPanel.ts`, `findingsPanel.test.ts`, `extension.ts`, `package.json`, README, and task STATUS — no dismiss re-implementation (SP-006).

```json
{"verdict":"PASS","feedback":"Confidence and category filters implemented with settings, hide policy, panel message, tests, and README. Contract checks pass; compile and 93 tests succeed with adequate coverage on changed modules."}
```
