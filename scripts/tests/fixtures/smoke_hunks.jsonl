{"id": "smoke-001", "file": "src/auth/login.go", "hunk": "@@ -10,3 +10,5 @@ func Login(ctx context.Context, creds Credentials) error {\n \tuser, err := store.FindByEmail(ctx, creds.Email)\n \tif err != nil {\n-\t\treturn err\n+\t\treturn fmt.Errorf(\"lookup user: %w\", err)\n \t}\n+\tif user == nil {\n+\t\treturn ErrInvalidCredentials\n+\t}", "source": "live", "repo": "stet"}
{"id": "smoke-002", "file": "src/crypto/tls.go", "hunk": "@@ -1,2 +1,2 @@\n-func compare(a, b []byte) bool { return bytes.Equal(a, b) }\n+func compare(a, b []byte) bool { return subtle.ConstantTimeCompare(a, b) == 1 }", "source": "live", "repo": "stet"}
{"id": "smoke-003", "file": "scripts/optimizer/token.py", "hunk": "@@ -18,4 +18,6 @@ def load_token(path: str) -> str:\n     with open(path, encoding=\"utf-8\") as handle:\n-        return handle.read().strip()\n+        raw = handle.read().strip()\n+    if not raw:\n+        raise ValueError(f\"empty token file: {path}\")\n+    return raw", "source": "live", "repo": "stet"}
{"id": "smoke-004", "file": "extension/src/review.ts", "hunk": "@@ -42,3 +42,5 @@ export async function parseFindings(raw: string): Promise<Finding[]> {\n   const parsed = JSON.parse(raw);\n-  return parsed as Finding[];\n+  if (!Array.isArray(parsed)) {\n+    throw new Error(\"expected findings array\");\n+  }\n+  return parsed as Finding[];", "source": "live", "repo": "stet"}
{"id": "smoke-005", "file": "cli/wasm/src/cache.rs", "hunk": "@@ -55,7 +55,7 @@ impl SessionCache {\n     pub fn get(&self, key: &str) -> Option<Vec<u8>> {\n-        let guard = self.inner.lock().unwrap();\n+        let guard = self.inner.lock().ok()?;\n         guard.get(key).cloned()\n     }\n }", "source": "converted", "repo": "acme/wasm-tools"}
